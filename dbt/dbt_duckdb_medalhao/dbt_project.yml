name: 'dbt_duckdb_medalhao'
version: '1.0.0'

profile: 'dbt_duckdb_medalhao'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

seeds:
  dbt_duckdb_medalhao:
    +schema: landing_zone

models:

  dbt_duckdb_medalhao:

    landing_zone:
      +materialized: view
      +schema: landing_zone
    
    bronze:
      +materialized: view
      +schema: bronze

    silver:
      +materialized: table
      +schema: silver

    gold:
      +materialized: table
      +schema: gold